spring:
  application:
    name: userservice
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: nacos:8848
      discovery:
        cluster-name: HZ
      config:
        file-extension: yaml
